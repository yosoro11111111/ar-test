## 修复计划

### 1. 修复模型路径
**问题**：模型文件实际在 `/public/models/` 目录，但代码中使用的是 `/optimized_models/`

**修复文件**：
- `src/models/modelList.js` - 修改 `MODEL_BASE_PATH` 为 `/models`
- `src/App.jsx` - 修改 `handleSelectLocalModel` 中的路径

### 2. 优化动作面板布局
**需求**：搜索条 → 分类标签 → 动作列表

**修复文件**：
- `src/components/ActionPanel.jsx` - 重新排列布局顺序

### 3. 动作名中文化
**需求**：将英文动作名翻译成中文

**修复文件**：
- `src/data/vrmaActions.js` - 增强 `translateName` 函数，添加更多英文到中文的映射

### 4. 验证步骤
1. 刷新页面，检查模型是否能正常加载
2. 打开动作面板，检查布局是否正确
3. 检查动作名是否显示为中文

确认后我将执行修复。