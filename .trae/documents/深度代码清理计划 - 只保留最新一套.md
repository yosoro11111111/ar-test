## 发现的主要问题

### 1. 多个动作系统（严重重复）
- `motionPackActions.js` - ✅ 当前主要使用
- `mmdActions.js` - ❌ 只有占位数据，删除
- `actions250.js` - ❌ 与主系统重复，删除
- `actionAnimations250.js` - ❌ 空数组，删除
- `poseLibrary.js` + `poseBoneData.js` - ⚠️ 合并为一个

### 2. 多个手势Hook
- `useGesture.js` - ✅ 当前使用
- `useSimpleGesture.js` - ❌ 未使用，删除
- `useGestureSystem.js` - ❌ 未使用，删除
- `useUnifiedInteraction.js` - ❌ 未使用，删除

### 3. 老的/废弃文件
- `ARSystem.patch.js` - ❌ 废弃补丁说明，删除
- `ModelDownloader.jsx` - ❌ 空实现，删除
- `VideoRecorder.jsx` - ❌ 空实现，删除
- `ShareCardGenerator.jsx` - ❌ 空实现，删除

### 4. App.jsx 问题
- 引用了 `setShowBoneEditor` 但未定义状态
- 需要清理未使用的导入

## 清理计划

### 第一阶段：删除废弃文件
1. 删除 `src/data/mmdActions.js`
2. 删除 `src/data/actions250.js`
3. 删除 `src/data/actionAnimations250.js`
4. 删除 `src/data/ARSystem.patch.js`
5. 删除 `src/hooks/useSimpleGesture.js`
6. 删除 `src/hooks/useGestureSystem.js`
7. 删除 `src/hooks/useUnifiedInteraction.js`
8. 删除 `src/components/ModelDownloader.jsx`
9. 删除 `src/components/VideoRecorder.jsx`
10. 删除 `src/components/ShareCardGenerator.jsx`

### 第二阶段：合并重复数据
1. 合并 `poseLibrary.js` + `poseBoneData.js` -> `poses.js`
2. 更新所有引用

### 第三阶段：修复 App.jsx
1. 删除未使用的导入
2. 修复 `setShowBoneEditor` 未定义问题
3. 删除未使用的状态

### 第四阶段：修复 ARSystem.jsx
1. 删除对废弃数据文件的导入
2. 统一使用 `motionPackActions.js`
3. 删除内嵌的 BoneEditor，使用独立组件

### 第五阶段：最终测试
1. 运行构建检查错误
2. 测试所有功能

请确认此计划后，我将开始执行清理。