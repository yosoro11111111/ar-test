## 修复内容

### 1. 恢复原有界面结构
- 恢复人物选择加载界面（文件上传/角色库选择）
- 恢复启动画面 → 选择界面 → 3D场景流程
- 默认使用原有ARSystem，不强制UnifiedARSystem

### 2. 恢复原有UI布局
- **顶部状态栏**: Logo + 版本 + 帮助/设置/截图/AR按钮
- **底部动作栏**: 搜索框 + 分类标签 + 动作按钮网格
- **录制按钮**: 恢复到顶部状态栏右侧

### 3. AR模式优化（参考Cotton）
- **加载优化**: 预加载、Web Worker、渐进式显示
- **平面检测可视化**: 显示检测到的平面网格
- **Hit Test指示器**: 准星/圆圈指示放置位置
- **手势操作**: 提示和反馈
- **性能优化**: 降低分辨率、简化材质、限制帧率

### 4. 代码结构
```
App.jsx
├── 启动画面
├── 人物选择界面（恢复）
└── ARSystem（摄像头模式）
    ├── 顶部状态栏（恢复）
    ├── 3D场景
    ├── 底部动作栏（恢复）
    └── AR按钮 → ARViewer
        ├── WebXR会话
        ├── 平面检测可视化
        └── 扫描进度
```

### 5. 修改文件
1. App.jsx - 恢复人物选择流程
2. ARSystem.jsx - 恢复UI布局，集成Hooks
3. ARViewer.jsx - 优化AR体验
4. 新建平面检测可视化组件

请确认后开始实现。