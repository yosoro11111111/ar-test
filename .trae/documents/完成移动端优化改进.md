# 完成移动端优化改进计划

## 需要完成的内容

### 1. 在 App.jsx 中集成 MobileDock
**目标**: 替换现有的底部控制栏，使用新的赛博朋克风格 MobileDock

**修改文件**: src/App.jsx
- 导入 MobileDock 组件
- 替换现有的底部按钮组
- 连接四个功能入口：动作、拍照、设置、特效

---

### 2. ActionPanel 使用 motionPackActions
**目标**: 使用用户的 FBX 动作文件，替换原有的动作数据

**修改文件**: src/components/ActionPanel.jsx
- 导入 motionPackActions
- 替换 actions 数据源
- 实现动作懒加载（点击时才加载FBX）

---

### 3. ARSystem 集成简化手势系统
**目标**: 使用 useSimpleGesture 替换现有的复杂手势

**修改文件**: src/components/ARSystem.jsx
- 导入 useSimpleGesture
- 替换现有的手势处理逻辑
- 只保留：单指旋转、双指缩放、双指平移
- 禁用所有点击、长按、双击

---

### 4. 添加手势提示
**目标**: 用户操作时显示手势提示

**修改文件**: src/components/ARSystem.jsx
- 导入 GestureIndicator
- 在手势操作时显示提示

---

## 实施顺序
1. 先完成 MobileDock 集成（用户能立即看到新UI）
2. 然后 ActionPanel 使用 motionPackActions
3. 最后 ARSystem 手势系统优化

请确认后我将开始实施。