# 🎬 MMD Studio 大更新方案

## 📋 更新概述

本次更新将对 MMD 制造器进行全面重构，包含项目系统、资源管理、界面重做、功能增强等多个方面。

---

## 🎯 核心目标

1. **项目系统**：完整的导入导出功能（.ymmdpack）
2. **资源管理**：支持本地引用和嵌入资源
3. **数据包系统**：通用资源包（.smmdpack）
4. **场景系统**：支持视频/图片/GLB/纯色背景
5. **道具系统**：GLB 道具 + 内置动画
6. **界面重做**：三栏布局，操作简化
7. **时间轴优化**：轨道合并，多线播放
8. **导出优化**：多种格式，多分辨率

---

## 📦 一、项目系统（Project System）

### 1.1 项目文件格式（.ymmdpack）

```
project.ymmdpack (ZIP压缩包)
├── manifest.json          # 项目元数据
├── timeline.json          # 时间轴数据
├── settings.json          # 项目设置
├── assets/                # 内嵌资源文件夹
│   ├── characters/
│   │   └── character1.vrm
│   ├── motions/
│   │   └── dance1.vrma
│   ├── props/
│   │   └── sword.glb
│   ├── scenes/
│   │   └── room.glb
│   ├── music/
│   │   └── bgm.mp3
│   └── videos/
│       └── background.mp4
└── thumbnail.jpg          # 项目缩略图
```

### 1.2 资源引用策略

```typescript
interface AssetReference {
  id: string                    // 唯一标识
  type: AssetType               // 资源类型
  name: string                  // 显示名称
  refType: 'local' | 'embedded' // 引用类型
  // local: 本地文件路径
  localPath?: string
  // embedded: 项目内嵌
  embeddedPath?: string
  // 文件信息
  size: number
  format: string
  thumbnail?: string
}

type AssetType = 
  | 'vrm'      // 角色模型
  | 'vrma'     // 动作文件
  | 'glb'      // GLB模型（道具/场景）
  | 'mp3'      // 音频
  | 'mp4'      // 视频
  | 'jpg'      // 图片
```

### 1.3 导入导出逻辑

**导出时**：
- 本地引用的文件：询问是否嵌入
- 嵌入的文件：打包到 assets/ 目录
- 生成完整的项目清单

**导入时**：
- 解压 .ymmdpack 文件
- 加载 manifest.json
- 提取内嵌资源到内存/IndexedDB
- 检查本地文件引用是否存在

---

## 📦 二、数据包系统（Data Package System）

### 2.1 数据包格式（.smmdpack）

```
data.smmdpack (ZIP压缩包)
├── manifest.json          # 数据包清单
├── characters/            # 角色模型
│   ├── character1.vrm
│   └── character2.vrm
├── motions/               # 动作文件
│   ├── dance1.vrma
│   └── dance2.vrma
├── props/                 # 道具模型
│   ├── sword.glb
│   └── shield.glb
├── scenes/                # 场景模型
│   ├── room.glb
│   └── stage.glb
├── music/                 # 音乐文件
│   ├── bgm1.mp3
│   └── bgm2.mp3
└── videos/                # 视频背景
    ├── bg1.mp4
    └── bg2.mp4
```

### 2.2 数据包清单格式

```json
{
  "version": "1.0",
  "type": "smmd-data-package",
  "name": "MMD基础资源包",
  "description": "包含常用角色、动作、道具和场景",
  "author": "作者名",
  "createdAt": "2024-01-01T00:00:00Z",
  "categories": {
    "characters": [
      {"name": "角色1", "file": "characters/character1.vrm", "thumbnail": "thumbs/char1.jpg"}
    ],
    "motions": [
      {"name": "舞蹈1", "file": "motions/dance1.vrma", "category": "舞蹈"}
    ],
    "props": [
      {"name": "剑", "file": "props/sword.glb", "hasAnimation": false}
    ],
    "scenes": [
      {"name": "房间", "file": "scenes/room.glb", "type": "indoor"}
    ],
    "music": [
      {"name": "背景音乐1", "file": "music/bgm1.mp3", "duration": 180}
    ]
  }
}
```

### 2.3 数据包管理

- **导入数据包**：拖拽或选择 .smmdpack 文件
- **数据包列表**：显示已导入的数据包
- **资源浏览**：按分类浏览数据包内资源
- **资源使用**：直接从数据包选择资源使用

---

## 🎬 三、场景系统重构（Scene System）

### 3.1 支持的背景类型

```typescript
interface SceneConfig {
  type: 'video' | 'image' | 'glb' | 'color' | 'transparent'
  
  // video 类型
  videoSrc?: string
  videoLoop?: boolean
  videoVolume?: number
  
  // image 类型
  imageSrc?: string
  imageFit?: 'cover' | 'contain' | 'stretch'
  
  // glb 类型
  glbSrc?: string
  glbScale?: number
  glbPosition?: {x, y, z}
  
  // color 类型
  color?: string
  
  // 通用设置
  brightness?: number
  contrast?: number
  saturation?: number
}
```

### 3.2 场景功能

- **视频背景**：支持播放控制、循环、音量
- **图片背景**：支持缩放、平移、适配模式
- **GLB场景**：支持模型加载、灯光、相机位
- **纯色背景**：颜色选择器
- **透明背景**：用于后期合成

### 3.3 移除的功能

- ❌ AR 拍照生成场景
- ❌ WebXR 场景录制
- ❌ AR 场景预览

---

## 🎭 四、角色系统优化

### 4.1 角色管理

- 从数据包加载 VRM 角色
- 支持本地 VRM 文件导入
- 角色缩略图显示
- 角色预设（默认姿势、表情）

### 4.2 角色属性

```typescript
interface CharacterConfig {
  id: string
  name: string
  modelPath: string
  
  // 变换
  position: {x, y, z}
  rotation: {x, y, z}
  scale: number
  
  // 材质
  materials?: MaterialConfig[]
  
  // 初始状态
  initialExpression?: string
  initialPose?: string
}
```

---

## 📦 五、道具系统增强（Prop System）

### 5.1 道具来源

1. **public/object 文件夹**：41 个预设道具
2. **数据包导入**：从 .smmdpack 加载
3. **本地导入**：拖拽 GLB 文件

### 5.2 道具功能

```typescript
interface PropConfig {
  id: string
  name: string
  modelPath: string
  
  // 变换
  position: {x, y, z}
  rotation: {x, y, z}
  scale: {x, y, z}
  
  // 动画
  hasAnimation: boolean
  animationName?: string
  animationLoop?: boolean
  
  // 绑定
  attachTo?: string        // 绑定到的角色ID
  attachPoint?: string     // 绑定点：hand, head, back等
  attachOffset?: {x, y, z} // 偏移量
  
  // 物理
  physics?: boolean
  gravity?: boolean
}
```

### 5.3 道具操作

- **独立动画**：播放道具内置动画
- **角色绑定**：绑定到角色骨骼（手持、头戴等）
- **关键帧动画**：位置/旋转/缩放关键帧
- **物理模拟**：简单的重力、碰撞

---

## ⏱️ 六、时间轴系统优化（Timeline System）

### 6.1 轨道合并

**原轨道（9种）→ 新轨道（6种）**

| 新轨道 | 包含原轨道 | 说明 |
|--------|-----------|------|
| 场景轨道 | 场景 | 背景设置 |
| 角色轨道 | 动作+表情+口型 | 角色动画整合 |
| 变换轨道 | 位置+旋转+缩放 | 空间变换整合 |
| 摄像机轨道 | 摄像机 | 机位和移动 |
| 道具轨道 | 道具 | 道具动画和变换 |
| 特效轨道 | 特效 | 粒子效果 |

### 6.2 轨道数据结构

```typescript
interface Track {
  id: string
  type: TrackType
  name: string
  targetId: string      // 关联的角色/道具ID
  clips: Clip[]
  muted: boolean
  locked: boolean
}

interface Clip {
  id: string
  type: ClipType
  startTime: number     // 开始时间（秒）
  duration: number      // 时长（秒）
  
  // 动作片段
  actionName?: string
  actionPath?: string
  loop?: boolean
  speed?: number
  
  // 变换片段
  startPosition?: {x, y, z}
  endPosition?: {x, y, z}
  startRotation?: {x, y, z}
  endRotation?: {x, y, z}
  easing?: 'linear' | 'easeIn' | 'easeOut' | 'easeInOut'
  
  // 其他属性...
}
```

### 6.3 多线播放

- 每个角色独立动画混合器
- 全局时间同步
- 支持角色间交互
- 性能优化（只播放可见片段）

### 6.4 时间轴界面优化

- **轨道折叠**：点击角色名折叠/展开轨道
- **片段编辑**：双击编辑，右键菜单
- **拖拽调整**：拖拽移动片段，边缘拉伸调整时长
- **缩放控制**：鼠标滚轮或按钮缩放时间轴
- **时间标记**：添加标记点，快速跳转

---

## 🎨 七、界面重设计方案

### 7.1 整体布局

```
┌─────────────────────────────────────────────────────────────────┐
│ 🎬 MMD Studio                              [💾] [📂] [📦] [⚙️] │
│                              保存   打开   数据包  设置          │
├─────────────────────────────────────────────────────────────────┤
│  📦 当前数据包: [默认资源包 ▼]  [+ 导入数据包]                   │
├──────────┬─────────────────────────────┬────────────────────────┤
│          │                             │                        │
│ 📁 资源   │      🎥 3D 预览区            │     ⚙️ 属性面板        │
│ 面板      │                             │                        │
│          │   ┌─────────────────────┐   │  ┌──────────────────┐  │
│ 🔍 [搜索] │   │                     │   │  │ 📍 变换          │  │
│          │   │   场景渲染画面       │   │  │ 位置 X Y Z       │  │
├──────────┤   │                     │   │  │ 旋转 X Y Z       │  │
│ 👤 角色   │   │  [摄像机控制工具]    │   │  │ 缩放 X Y Z       │  │
│ [+ 导入]  │   │                     │   │  └──────────────────┘  │
│ • 角色1   │   │  [坐标显示]          │   │                        │
│ • 角色2   │   │  [FPS显示]           │   │  ┌──────────────────┐  │
├──────────┤   │                     │   │  │ 🎭 动画          │  │
│ 📦 道具   │   └─────────────────────┘   │  │ [动作选择 ▼]      │  │
│ [+ 导入]  │                             │  │ [播放] [暂停]     │  │
│ • 道具1   │                             │  │ 进度: ████░░░░░   │  │
│ • 道具2   │                             │  └──────────────────┘  │
├──────────┤                             │                        │
│ 🎬 场景   │                             │  ┌──────────────────┐  │
│ [+ 导入]  │                             │  │ 🎨 材质          │  │
│ • 场景1   │                             │  │ [颜色选择]        │  │
├──────────┤                             │  │ [贴图选择]        │  │
│ 🎭 动作   │                             │  └──────────────────┘  │
│ [+ 导入]  │                             │                        │
├──────────┤                             │  ┌──────────────────┐  │
│ 🎵 音乐   │                             │  │ 📊 关键帧        │  │
│ [+ 导入]  │                             │  │ [关键帧列表]      │  │
│ • 音乐1   │                             │  │ [+ 添加] [- 删除] │  │
└──────────┴─────────────────────────────┴────────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│ ⏱️ 时间轴                                                        │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ [角色A] ▼ │ [🎭动作:舞蹈1]────[🎭动作:舞蹈2]                 │ │
│ │           │ [📍位置:圆形]──[📍位置:直线]                    │ │
│ │ [角色B] ▼ │ [🎭动作:待机]────[🎭动作:挥手]                   │ │
│ │ [道具1] ▼ │ [📍位置:固定]──[📍位置:移动]                     │ │
│ │ [摄像机] ▼│ [📹机位:正面]────[📹机位:侧面]──[📹机位:全景]     │ │
│ │ [场景]  ▼ │ [🎬视频背景]                                       │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ◀  ▶  ⏸  ⏹  │  00:00:15 / 00:02:00  │  🔍+  🔍-  │  [导出▶]   │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 左侧面板 - 资源库

**功能**：
- 分类显示：角色、道具、场景、动作、音乐
- 搜索过滤：按名称搜索
- 标签筛选：按标签过滤
- 导入按钮：每类资源的导入入口
- 拖拽使用：拖拽资源到预览区或时间轴

**显示方式**：
- 网格视图：缩略图 + 名称
- 列表视图：详细信息

### 7.3 中央区域 - 3D预览

**功能**：
- 实时渲染预览
- 摄像机控制（旋转、平移、缩放）
- 视角切换（正面、侧面、顶部、自由）
- 坐标显示
- FPS显示
- 网格/辅助线开关

**工具栏**：
- 选择工具
- 移动工具
- 旋转工具
- 缩放工具
- 摄像机工具

### 7.4 右侧面板 - 属性编辑

**根据选中对象动态显示**：

**选中角色时**：
- 变换（位置、旋转、缩放）
- 动画（当前动作、播放控制）
- 材质（颜色、贴图）
- 表情（BlendShape）
- 骨骼（高级）

**选中道具时**：
- 变换
- 动画（如果有）
- 绑定设置
- 物理设置

**选中摄像机时**：
- 位置/目标点
- FOV
- 焦距
- 关键帧列表

**选中片段时**：
- 时间（开始、时长）
- 缓动函数
- 具体属性（根据片段类型）

### 7.5 底部区域 - 时间轴

**功能**：
- 轨道显示
- 片段显示和编辑
- 播放控制
- 时间显示
- 缩放控制

**交互**：
- 点击轨道：选中轨道
- 点击片段：选中片段，显示属性
- 双击片段：打开编辑弹窗
- 拖拽片段：移动时间位置
- 拖拽边缘：调整时长
- 右键菜单：删除、复制、粘贴

---

## 🚀 八、导出系统优化

### 8.1 导出格式

```typescript
interface ExportConfig {
  format: 'mp4' | 'gif' | 'png' | 'webm'
  
  // 分辨率
  resolution: '720p' | '1080p' | '2K' | '4K' | 'custom'
  customWidth?: number
  customHeight?: number
  
  // 视频设置
  fps: 30 | 60
  bitrate: 'low' | 'medium' | 'high' | number
  codec: 'h264' | 'h265' | 'vp9'
  
  // 质量
  quality: 'draft' | 'normal' | 'high' | 'ultra'
  
  // 音频
  includeAudio: boolean
  audioBitrate?: number
  
  // 透明背景
  transparent: boolean
  
  // 帧范围
  startFrame?: number
  endFrame?: number
}
```

### 8.2 导出选项

**视频导出**：
- 格式：MP4, WebM
- 分辨率：720p, 1080p, 2K, 4K
- 帧率：30fps, 60fps
- 质量：草稿、标准、高质量

**GIF导出**：
- 分辨率：自定义
- 颜色数：128, 256
- 帧率：10, 15, 20, 30
- 循环：永久、指定次数

**帧序列**：
- 格式：PNG, JPG
- 透明背景：是/否
- 命名规则：frame_0001.png

**项目导出**：
- 格式：.ymmdpack
- 包含：完整项目数据和内嵌资源

---

## 🛠️ 九、技术实现方案

### 9.1 核心架构

```
┌─────────────────────────────────────────┐
│           MMD Studio Application        │
├─────────────────────────────────────────┤
│  UI Layer (React + CSS Modules)         │
├─────────────────────────────────────────┤
│  State Management (Zustand/Context)     │
├─────────────────────────────────────────┤
│  Core Systems                           │
│  ├── ProjectManager     # 项目管理      │
│  ├── ResourceManager    # 资源管理      │
│  ├── DataPackageManager # 数据包管理    │
│  ├── TimelineEngine     # 时间轴引擎    │
│  ├── RenderEngine       # 渲染引擎      │
│  └── ExportEngine       # 导出引擎      │
├─────────────────────────────────────────┤
│  Three.js + VRM + VRMA                  │
├─────────────────────────────────────────┤
│  Storage Layer                          │
│  ├── IndexedDB          # 本地存储      │
│  ├── FileSystem API     # 文件系统      │
│  └── Memory Cache       # 内存缓存      │
└─────────────────────────────────────────┘
```

### 9.2 关键技术点

**项目系统**：
- 使用 JSZip 进行压缩/解压
- 使用 File System Access API（如果可用）
- 使用 IndexedDB 存储大文件

**资源管理**：
- Blob URL 管理
- 资源缓存策略
- 内存管理（及时释放）

**渲染优化**：
- 使用 requestAnimationFrame
- 离屏渲染（导出时）
- LOD（细节层次）

**性能优化**：
- 虚拟滚动（资源列表）
- 懒加载（缩略图）
- Web Worker（导出处理）

---

## 📅 十、实施计划

### 阶段1：基础架构（5天）
- [ ] 创建新的项目结构
- [ ] 实现 ProjectManager
- [ ] 实现 ResourceManager
- [ ] 实现 DataPackageManager
- [ ] 设计新的状态管理

### 阶段2：项目系统（3天）
- [ ] 实现 .ymmdpack 导入导出
- [ ] 资源引用管理
- [ ] 本地文件系统集成
- [ ] 项目设置界面

### 阶段3：数据包系统（2天）
- [ ] 实现 .smmdpack 格式
- [ ] 数据包导入界面
- [ ] 资源浏览和选择
- [ ] 默认数据包准备

### 阶段4：场景系统（2天）
- [ ] 视频背景支持
- [ ] GLB场景加载
- [ ] 场景设置界面
- [ ] 移除AR相关功能

### 阶段5：道具系统（2天）
- [ ] GLB道具加载
- [ ] 道具动画支持
- [ ] 角色绑定功能
- [ ] 道具关键帧

### 阶段6：界面重做（4天）
- [ ] 新布局框架
- [ ] 左侧面板（资源库）
- [ ] 右侧面板（属性）
- [ ] 底部时间轴
- [ ] 中央预览区

### 阶段7：时间轴优化（3天）
- [ ] 轨道合并
- [ ] 多线播放支持
- [ ] 片段编辑优化
- [ ] 快捷键支持

### 阶段8：导出优化（2天）
- [ ] 多种格式支持
- [ ] 多分辨率导出
- [ ] 导出进度显示
- [ ] 后台导出（Web Worker）

### 阶段9：功能整合（2天）
- [ ] 所有功能联调
- [ ] 数据流测试
- [ ] 边界情况处理

### 阶段10：测试优化（3天）
- [ ] 功能测试
- [ ] 性能测试
- [ ] 用户体验优化
- [ ] Bug修复

**总计：约28天**

---

## 📁 十一、文件结构

```
src/components/mmd/
├── MMDStudio/                    # 主应用
│   ├── index.jsx                 # 入口组件
│   ├── MMDStudio.module.css      # 主样式
│   ├── layout/
│   │   ├── MainLayout.jsx        # 主布局
│   │   ├── LeftPanel.jsx         # 左侧面板
│   │   ├── CenterPanel.jsx       # 中央预览
│   │   ├── RightPanel.jsx        # 右侧面板
│   │   └── BottomPanel.jsx       # 底部时间轴
│   ├── panels/
│   │   ├── ResourcePanel/        # 资源面板
│   │   ├── PreviewPanel/         # 预览面板
│   │   ├── PropertyPanel/        # 属性面板
│   │   └── TimelinePanel/        # 时间轴面板
│   ├── systems/
│   │   ├── ProjectManager.js     # 项目管理
│   │   ├── ResourceManager.js    # 资源管理
│   │   ├── DataPackageManager.js # 数据包管理
│   │   ├── TimelineEngine.js     # 时间轴引擎
│   │   ├── RenderEngine.js       # 渲染引擎
│   │   └── ExportEngine.js       # 导出引擎
│   ├── components/
│   │   ├── Character/            # 角色相关组件
│   │   ├── Prop/                 # 道具相关组件
│   │   ├── Scene/                # 场景相关组件
│   │   ├── Camera/               # 摄像机相关组件
│   │   └── Common/               # 通用组件
│   └── hooks/
│       ├── useProject.js         # 项目Hook
│       ├── useResources.js       # 资源Hook
│       ├── useTimeline.js        # 时间轴Hook
│       └── useExport.js          # 导出Hook
├── shared/                       # 共享组件
└── utils/                        # 工具函数
```

---

## ✅ 十二、功能检查清单

### 项目系统
- [ ] 创建新项目
- [ ] 保存项目
- [ ] 打开项目
- [ ] 导出 .ymmdpack
- [ ] 导入 .ymmdpack
- [ ] 资源引用管理
- [ ] 内嵌资源打包
- [ ] 项目缩略图

### 数据包系统
- [ ] 导入 .smmdpack
- [ ] 浏览数据包资源
- [ ] 从数据包选择资源
- [ ] 多个数据包管理
- [ ] 默认数据包

### 资源导入
- [ ] 导入 VRM 角色
- [ ] 导入 VRMA 动作
- [ ] 导入 GLB 道具
- [ ] 导入 GLB 场景
- [ ] 导入 MP3 音乐
- [ ] 导入 MP4 视频
- [ ] 导入 JPG/PNG 图片
- [ ] 拖拽导入
- [ ] 批量导入

### 场景系统
- [ ] 视频背景
- [ ] 图片背景
- [ ] GLB 场景
- [ ] 纯色背景
- [ ] 透明背景
- [ ] 场景切换
- [ ] 场景设置

### 角色系统
- [ ] 加载 VRM 角色
- [ ] 角色变换
- [ ] 动作播放
- [ ] 表情控制
- [ ] 材质编辑
- [ ] 角色删除

### 道具系统
- [ ] 加载 GLB 道具
- [ ] 道具变换
- [ ] 道具动画
- [ ] 角色绑定
- [ ] 关键帧动画
- [ ] 道具删除

### 时间轴系统
- [ ] 添加轨道
- [ ] 删除轨道
- [ ] 添加片段
- [ ] 删除片段
- [ ] 移动片段
- [ ] 调整时长
- [ ] 片段编辑
- [ ] 播放控制
- [ ] 时间缩放
- [ ] 多线播放

### 摄像机系统
- [ ] 机位切换
- [ ] 关键帧动画
- [ ] FOV 调整
- [ ] 预览显示

### 导出系统
- [ ] 导出 MP4
- [ ] 导出 GIF
- [ ] 导出帧序列
- [ ] 导出项目
- [ ] 多分辨率
- [ ] 多质量
- [ ] 透明背景
- [ ] 进度显示

### 界面功能
- [ ] 三栏布局
- [ ] 资源面板
- [ ] 属性面板
- [ ] 时间轴
- [ ] 预览区
- [ ] 工具栏
- [ ] 快捷键
- [ ] 主题切换

---

## 🎨 十三、界面设计稿

### 主界面
```
┌──────────────────────────────────────────────────────────────┐
│ 🎬 MMD Studio                                    💾 📂 📦 ⚙️ │
├──────────────────────────────────────────────────────────────┤
│ 📦 默认资源包 ▼                              [+ 导入数据包] │
├───────────┬─────────────────────────────┬────────────────────┤
│           │                             │                    │
│  📁 资源   │                             │     ⚙️ 属性        │
│           │      🎥 3D 预览区            │                    │
│ 🔍 搜索    │                             │  ┌──────────────┐  │
│           │   ┌─────────────────────┐   │  │ 📍 变换       │  │
│ 👤 角色   │   │                     │   │  │              │  │
│ [+导入]   │   │                     │   │  │ 位置 X Y Z   │  │
│ ├ 角色1   │   │    3D 场景渲染      │   │  │ 旋转 X Y Z   │  │
│ └ 角色2   │   │                     │   │  │ 缩放 X Y Z   │  │
│           │   │  [工具栏]            │   │  └──────────────┘  │
│ 📦 道具   │   │                     │   │                    │
│ [+导入]   │   │  坐标: 0, 0, 0      │   │  ┌──────────────┐  │
│ ├ 道具1   │   │  FPS: 60            │   │  │ 🎭 动画       │  │
│ └ 道具2   │   │                     │   │  │              │  │
│           │   └─────────────────────┘   │  │ [动作选择 ▼]  │  │
│ 🎬 场景   │                             │  │ [▶] [⏸] [⏹]│  │
│ [+导入]   │                             │  │ 进度 ████░░░  │  │
│ ├ 场景1   │                             │  └──────────────┘  │
│ └ 场景2   │                             │                    │
│           │                             │  ┌──────────────┐  │
│ 🎭 动作   │                             │  │ 📊 关键帧     │  │
│ [+导入]   │                             │  │              │  │
│ ├ 动作1   │                             │  │ [+添加]      │  │
│ └ 动作2   │                             │  │ [-删除]      │  │
│           │                             │  └──────────────┘  │
│ 🎵 音乐   │                             │                    │
│ [+导入]   │                             │                    │
│ └ 音乐1   │                             │                    │
└───────────┴─────────────────────────────┴────────────────────┘
┌──────────────────────────────────────────────────────────────┐
│ ⏱️ 时间轴                                                     │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 角色A ▼ │ 🎭舞蹈1 ████████ 🎭舞蹈2 ████████              │ │
│ │         │ 📍圆形 ████ 📍直线 ████                       │ │
│ │ 角色B ▼ │ 🎭待机 ████████████ 🎭挥手 ████                │ │
│ │ 道具1 ▼ │ 📍固定 ████ 📍移动 ██████████                  │ │
│ │ 摄像机 ▼│ 📹正面 ████ 📹侧面 ████ 📹全景 ████            │ │
│ │ 场景   ▼│ 🎬视频背景 ████████████████████████            │ │
│ └──────────────────────────────────────────────────────────┘ │
│ ◀  ▶  ⏸  ⏹  │ 00:00:15/00:02:00 │ 🔍+ 🔍- │ [导出 ▼]      │
└──────────────────────────────────────────────────────────────┘
```

### 工具栏
```
[选择 🔍] [移动 ↔️] [旋转 🔄] [缩放 🔍] | [摄像机 📹] | [网格 ☐] [辅助线 ☐]
```

### 片段编辑弹窗
```
┌─────────────────────────┐
│ 编辑片段 - 动作          │
├─────────────────────────┤
│ 动作: [舞蹈1        ▼]  │
│ 开始: [00:00:05]        │
│ 时长: [00:00:10]        │
│ 速度: [1.0x] [--o--]    │
│ 循环: [✓]               │
│ 缓动: [缓入缓出    ▼]   │
├─────────────────────────┤
│      [取消] [确定]      │
└─────────────────────────┘
```

### 导出设置弹窗
```
┌─────────────────────────┐
│ 导出设置                 │
├─────────────────────────┤
│ 格式: [MP4          ▼]  │
│ 分辨率: [1080p      ▼]  │
│ 帧率: [30fps        ▼]  │
│ 质量: [高质量       ▼]  │
│ 透明背景: [ ]           │
│ 包含音频: [✓]           │
├─────────────────────────┤
│ 预计大小: 150 MB        │
│ 预计时长: 2:00          │
├─────────────────────────┤
│ [取消]        [导出 ▶]  │
└─────────────────────────┘
```

---

## 📝 十四、注意事项

### 兼容性
- 使用现代浏览器 API（Chrome/Edge/Firefox/Safari 最新版）
- 提供降级方案（如 File System API 不可用时）

### 性能
- 大文件使用流式处理
- 及时释放 Blob URL
- 使用 Web Worker 进行导出

### 安全
- 不自动执行下载的文件
- 沙箱环境运行用户脚本
- 敏感操作需要确认

### 用户体验
- 提供详细的操作提示
- 进度显示（加载、导出）
- 撤销/重做支持
- 自动保存

---

## 🎉 总结

本次更新将 MMD Studio 从一个简单的 AR MMD 工具升级为完整的桌面级 MMD 制作软件，具备：

- ✅ 完整的项目管理
- ✅ 强大的资源管理
- ✅ 丰富的场景支持
- ✅ 灵活的道具系统
- ✅ 专业的时间轴
- ✅ 友好的用户界面
- ✅ 多样的导出选项

最终目标是打造一个**好用、易用、功能强大**的 MMD 制作工具。

---

*文档版本: 1.0*
*创建日期: 2024年*
*作者: AI Assistant*
